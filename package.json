{
  "name": "safespot",
  "version": "1.4.0",
  "private": true,
  "description": "SafeSpot local-first safety training platform",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install",
    "dev": "concurrently -k -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run build && npm run start:prod --prefix server",
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "test": "npm test --prefix server",
    "backup": "node server/src/backup-cli.js backup",
    "restore": "node server/src/backup-cli.js restore"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
